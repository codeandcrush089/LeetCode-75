# Problem 605: Can Place Flowers
![Easy](https://img.shields.io/badge/Level-Easy-brightgreen?style=for-the-badge) [![Problem Link](https://img.shields.io/badge/LeetCode-Link-orange?style=for-the-badge&logo=leetcode)](https://leetcode.com/problems/can-place-flowers/description/)

## 🐍 Python Solution

```python
class Solution:
  def canPlaceFlowers(self, flowerbed: list[int], n: int) -> bool:
    for i, flower in enumerate(flowerbed):
      if flower == 0 and (
              i == 0 or flowerbed[i - 1] == 0) and (
              i == len(flowerbed) - 1 or flowerbed[i + 1] == 0):
        flowerbed[i] = 1
        n -= 1
      if n <= 0:
        return True

    return False
```

<details>
<summary><b>🟦 C++ Solution</b></summary>
  
```cpp
class Solution {
 public:
  bool canPlaceFlowers(vector<int>& flowerbed, int n) {
    if (n == 0)
      return true;

    for (int i = 0; i < flowerbed.size(); ++i)
      if (flowerbed[i] == 0 && (i == 0 || flowerbed[i - 1] == 0) &&
          (i == flowerbed.size() - 1 || flowerbed[i + 1] == 0)) {
        flowerbed[i] = 1;
        if (--n == 0)
          return true;
      }

    return false;
  }
};
```

</details>

<details>
<summary><b>☕ Java Solution</b></summary>
  
```java
class Solution {
  public boolean canPlaceFlowers(int[] flowerbed, int n) {
    if (n == 0)
      return true;

    for (int i = 0; i < flowerbed.length; ++i)
      if (flowerbed[i] == 0 && (i == 0 || flowerbed[i - 1] == 0) &&
          (i == flowerbed.length - 1 || flowerbed[i + 1] == 0)) {
        flowerbed[i] = 1;
        if (--n == 0)
          return true;
      }

    return false;
  }
}
```
</details>

---
## 🔹 Approach Used

* **Greedy Traversal Approach**
* Traverse the flowerbed from left to right.
* At each position, check if a flower can be planted:

  * Current plot is empty.
  * Left neighbor (if exists) is empty.
  * Right neighbor (if exists) is empty.
* If possible, plant a flower and decrement `n`.
* If `n` becomes zero or less, return `True`; otherwise, return `False` after traversal.



